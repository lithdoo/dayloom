name: CLI

on:
  push:
    paths:
      - 'packages/cli/**'
      - 'packages/archive-protocol/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/cli.yml'
  pull_request:
    paths:
      - 'packages/cli/**'
      - 'packages/archive-protocol/**'
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/cli.yml'

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        node: [20, 22]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node }}
          cache: npm
      - run: npm ci
      - run: npm test -w @dayloom/archive-protocol
      - run: npm test -w @dayloom/cli
