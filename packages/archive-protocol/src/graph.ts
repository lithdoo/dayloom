import { protocolError } from './errors';import { parseArchiveCommitV2,type ArchiveCommitV2 } from './commit';
export function walkCommitAncestorsV2(startId:string,commits:ReadonlyMap<string,ArchiveCommitV2>):readonly ArchiveCommitV2[]{const result:ArchiveCommitV2[]=[];const seen=new Set<string>();let id:string|null=startId;while(id!==null){if(seen.has(id))protocolError('ARCHIVE_PROTOCOL_REFERENCE_INVALID','Commit parent graph contains a cycle.',{id});seen.add(id);const raw=commits.get(id);if(!raw)protocolError('ARCHIVE_PROTOCOL_REFERENCE_INVALID','Commit parent is missing.',{id});const commit=parseArchiveCommitV2(raw);if(commit.id!==id)protocolError('ARCHIVE_PROTOCOL_REFERENCE_INVALID','Commit map key does not match commit id.',{id});result.push(commit);id=commit.parentCommitId;}return Object.freeze(result);}
export function validateCommitParentGraphV2(commits:ReadonlyMap<string,ArchiveCommitV2>):void{for(const id of commits.keys())walkCommitAncestorsV2(id,commits);}
