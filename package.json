{
  "name": "dayloom-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build -w @dayloom/core && npm run build -w @dayloom/cli",
    "test": "npm run test -w @dayloom/core"
  },
  "engines": {
    "node": ">=18"
  }
}
