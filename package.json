{
  "name": "dayloom-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build -w @dayloom/core && npm run build -w @dayloom/core2 && npm run build -w @dayloom/cli && npm run build -w @dayloom/tui",
    "test": "npm run test -w @dayloom/core && npm run test -w @dayloom/core2 && npm run test -w @dayloom/cli && npm run test -w @dayloom/tui"
  },
  "engines": {
    "node": ">=18"
  }
}
