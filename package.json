{
  "name": "dayloom-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build -w @dayloom/archive-protocol && npm run build -w @dayloom/cli",
    "test": "npm run test -w @dayloom/archive-protocol && npm run test -w @dayloom/cli",
    "cli": "node packages/cli/dist/main.js",
    "tui": "node packages/tui/dist/main.js",
    "docs:dev": "vitepress dev doc",
    "docs:check": "node scripts/check-docs.mjs",
    "examples:check": "node scripts/check-examples.mjs",
    "docs:build": "npm run docs:check && vitepress build doc",
    "docs:preview": "vitepress preview doc"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  },
  "engines": {
    "node": ">=20"
  }
}
