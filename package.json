{
  "name": "dayloom-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build": "npm run build -w @dayloom/archive-protocol && npm run build -w @dayloom/core-old && npm run build -w @dayloom/core && npm run build -w @dayloom/core2 && npm run build -w @dayloom/cli && npm run build -w @dayloom/tui-old && npm run build -w @dayloom/tui",
    "test": "npm run test -w @dayloom/archive-protocol && npm run test -w @dayloom/core-old && npm run test -w @dayloom/core && npm run test -w @dayloom/core2 && npm run test -w @dayloom/cli && npm run test -w @dayloom/tui-old && npm run test -w @dayloom/tui",
    "tui": "node packages/tui/dist/main.js",
    "tui:old": "node packages/tui-old/dist/main.js",
    "docs:dev": "vitepress dev doc",
    "docs:check": "node scripts/check-docs.mjs",
    "docs:build": "npm run docs:check && vitepress build doc",
    "docs:preview": "vitepress preview doc"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  },
  "engines": {
    "node": ">=20"
  }
}
