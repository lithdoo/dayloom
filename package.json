{
  "name": "dayloom-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w dayloom",
    "test": "npm run test -w dayloom"
  },
  "engines": {
    "node": ">=18"
  }
}
